---
title: Button
desc: Button component
---

import { Button } from '@literal-ui/core'
import { MdCheck } from 'react-icons/md'

# `import { Button } from '@literal-ui/core'{:ts}`

## `variant: "filled" | "outlined" | "text" | "elevated" | "tonal"{:ts}`

<Demo>
  {Button.variants.map((variant) => (
    <Button key={variant} variant={variant}>
      {variant}
    </Button>
  ))}
</Demo>

```tsx
<Button variant={variant}>{variant}</Button>
```

## `disabled?: boolean{:ts}`

<Demo>
  {Button.variants.map((variant) => (
    <Button key={variant} variant={variant} disabled>
      {variant}
    </Button>
  ))}
</Demo>

```tsx
<Button variant={variant} disabled>
  {variant}
</Button>
```

## `Icon?: IconType{:ts}`

<Demo>
  {Button.variants.map((variant) => (
    <Button key={variant} variant={variant} Icon={MdCheck}>
      {variant}
    </Button>
  ))}
</Demo>

```tsx
<Button variant={variant} Icon={MdCheck}>
  {variant}
</Button>
```

## `as?: React.ElementType{:ts}`

<Demo>
  <Button
    variant="tonal"
    as="a"
    href="https://github.com"
    target="_blank"
    rel="noopener"
  >
    tonal
  </Button>
</Demo>

```tsx
<Button
  variant="tonal"
  as="a"
  href="https://github.com"
  target="_blank"
  rel="noopener"
>
  tonal
</Button>
```
